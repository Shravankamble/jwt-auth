# name: push docker image to ghcr
# on: 
#   push:
#     branches: ["master"]

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps: 
#     - uses: action/checkout@v3
#     - name: Push This image to ghcr
#       uses: docker/login-action@v1
#       registry: ghcr.io
